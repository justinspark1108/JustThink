import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "just think - Lateral Thinking Puzzles",
  description: "AI-powered lateral thinking puzzles. Test your reasoning with dark, challenging riddles generated on demand.",
  keywords: ["lateral thinking", "puzzles", "riddles", "brain teasers", "AI", "mystery", "logic"],
  authors: [{ name: "Justin Park" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "just think",
    description: "AI-powered lateral thinking puzzles",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
